# DEPLOY.md — قائمة النشر

**المشروع:** منصّة سامي للطباعة والنشر والتوزيع
**آخر تحديث:** 2 أوت 2026

> اتبعي هذه القائمة **بالترتيب** يوم النشر.
> كل بند 🔴 يجب تنفيذه — تجاهله يعني ثغرة أو فقدان بيانات.

---

## 🔴 قبل النشر — إجباري

### 1. تعطيل وضع الاختبار في الدخول

```
public/Front-Bouthaina/assets/js/login.js  →  السطر 5
```
```js
DEBUG: false,
```

> **إن بقيت `true`:** يتجاوز الدخول الخادم تماماً — أي كلمة سر تنجح
> لأي بريد. هذه أخطر ثغرة ممكنة في المشروع.

**التحقّق:** `Select-String -Path login.js -Pattern "DEBUG:"`

---

### 2. تعطيل وضع البيانات الوهمية

```
public/Front-Bouthaina/assets/js/api.js  →  السطر 8
```
```js
MOCK: false,
```

> بدون ذلك يقرأ الموقع من `/mocks` ويتجاهل الخادم كلياً.

---

### 3. منع الوصول إلى `/mocks/`

في `.htaccess` (النموذج في `.htaccess-sample`):
```apache
RewriteRule ^mocks/ - [F,L]
```

أو احذفي المجلد من نسخة الإنتاج.
`robots.txt` يمنع الفهرسة لكنه لا يمنع الوصول.

---

### 4. إعدادات Laravel

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://sami-library.com
```

> **`APP_DEBUG=true` في الإنتاج يعرض مسارات الملفات وبيانات
> قاعدة البيانات في صفحة الخطأ لأي زائر.**

وتأكّدي أن `APP_KEY` مولَّد: `php artisan key:generate`

---

### 5. حذف الحسابات والبيانات التجريبية

- [ ] حسابات الاختبار من `users`
- [ ] الكتب التجريبية من Seeders
- [ ] حساب أدمن حقيقي بكلمة سر قوية
- [ ] حذف أي توكن اختبار من `personal_access_tokens`

---

### 6. HTTPS إجباري

```apache
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

> بدونه تُرسَل كلمات المرور نصّاً واضحاً عبر الشبكة.

---

### 7. 🔴 النسخ الاحتياطي الآلي

**أهم بند في القائمة، وأكثر بند يُنسى.**

| ما يُنسخ | لماذا |
|---|---|
| قاعدة البيانات | الحسابات والكتب والمراجعات |
| `storage/app/private/books` | ملفات الكتب — **لا تُستعاد إن ضاعت** |

- [ ] نسخة يومية آلية
- [ ] تُحفظ **خارج** الخادم
- [ ] **جُرِّبت الاستعادة فعلاً** — نسخة لم تُجرَّب ليست نسخة

> ملفات الكتب هي المنتج نفسه. ضياعها يعني إعادة جمعها من المؤلفين
> واحداً واحداً — إن أمكن أصلاً.

---

## ⚙️ إعدادات الخادم

### 8. رفع حدود PHP

```ini
upload_max_filesize = 50M
post_max_size       = 55M
max_execution_time  = 300
```

> الافتراضي 2M و8M. بدون التعديل يفشل رفع الكتب عند 8 ميجا
> **مهما كُتب في Laravel**.

### 9. ربط صفحة الخطأ

```apache
ErrorDocument 404 /404.html
```

### 10. تخزين مؤقّت وضغط

انظري `.htaccess-sample` — CSS و JS شهراً، الصور ستة أشهر،
HTML بلا تخزين حتى تصل التحديثات فوراً.

---

## 🔖 تحسين الظهور

### 11. وسوم المشاركة الديناميكية

مطلوبة من الخادم — التفاصيل في `API.md` قسم «متطلّبات SEO».

> بدونها تظهر **كل** روابط الكتب في واتساب ببطاقة واحدة عامّة.
> والمشاركة جزء من عمل دار النشر.

### 12. تحديث الدومين

إن تغيّر عن `sami-library.com`، يُبدَّل في:

- [ ] `SITE` في `seo/apply_meta.py` ثم يُعاد تشغيله
- [ ] `robots.txt`
- [ ] `sitemap.xml`
- [ ] `.env` → `APP_URL`

### 13. خرائط الموقع الديناميكية

`/sitemap-books.xml` و `/sitemap-authors.xml` — يولّدهما الخادم.

### 14. تسجيل الموقع

- [ ] Google Search Console + إرسال `sitemap.xml`
- [ ] اختبار بطاقة المشاركة عبر Facebook Sharing Debugger

---

## ✅ اختبار ما بعد النشر

**المسار الكامل — يُنفَّذ على الموقع الحيّ:**

1. [ ] إنشاء حساب جديد
2. [ ] تسجيل خروج ودخول
3. [ ] تصفّح كتاب وتحميله
4. [ ] كتابة مراجعة → **لا تظهر فوراً** (تنتظر الموافقة)
5. [ ] الموافقة عليها من Filament → تظهر
6. [ ] الترقية إلى مؤلف
7. [ ] رفع كتاب → يصل الأدمن إشعار
8. [ ] الموافقة من Filament → يصل المؤلف إشعار → يظهر الكتاب
9. [ ] **رفض كتاب مع سبب** → يصل الإشعار بالسبب
10. [ ] حفظ كتاب في المكتبة ونقله بين الرفوف
11. [ ] نموذج التواصل
12. [ ] فتح رابط غير موجود → صفحة 404
13. [ ] مشاركة رابط كتاب في واتساب → تظهر البطاقة
14. [ ] تصفّح كامل من هاتف حقيقي

---

## 📦 التسليم للعميل

- [ ] بيانات دخول لوحة Filament
- [ ] **جلسة شرح**: كيف يرفع كتاباً · كيف يوافق · كيف يرفض مع سبب ·
      كيف يضيف قسماً · كيف يعدّل نصوص الصفحات
- [ ] توضيح: تعديل الأقسام لا يحتاج مبرمجاً — يتم من Filament
- [ ] اتفاق مكتوب: من يستضيف · من يدفع · هل هناك فترة دعم ومدّتها

---

## ⚠️ ما يبقى بانتظار العميل

| # | المطلوب | يعطّل |
|---|---|---|
| 1 | نصوص «من نحن» والرسالة والرؤية | `about.html` |
| 2 | شروط النشر بصيغتها النهائية | `about.html` |
| 3 | مراجعة قانونية لـ`terms` و`privacy` | النشر |
| 4 | بيانات التواصل الحقيقية | الفوتر و`about` |
| 5 | أغلفة الكتب | مظهر المكتبة |
| 6 | شعار عالي الجودة | كل الصفحات |

> النصوص المؤقّتة معلَّمة في الملفات بـ`<!-- ⚠️ نص مؤقت -->`.
> وتنبيه المسودّة في `terms` و`privacy` يُحذف بعد الاعتماد
> (ابحثي عن `lg-note`).
